"use client";

import { ArrowLeft, Building2, Download, Factory, Leaf, Loader2, MapPin, Sprout } from "lucide-react";
import { useCallback, useEffect, useMemo, useState } from "react";
import type { LocationAudit } from "@/lib/types";

type View = "residential" | "industrial" | "agriculture";
type ClimateRisk = { historical_period: { start_year: number; end_year: number; sample_years: number }; confidence: string; next_year_climate_baseline: { annual_mean_temperature_c: number; annual_precipitation_mean_mm: number }; historical_yearly_climate: Array<{ year: number; mean_temperature_c: number; total_precipitation_mm: number; maximum_daily_temperature_c: number; maximum_daily_wind_kmh: number }>; climate_trigger_likelihoods: Array<{ indicator: string; next_year_historical_likelihood_pct: number; historical_years_with_indicator: number; sample_years: number; interpretation: string }>; important_limit: string };
const VIEWS = { residential: { label: "Residential", icon: Building2 }, industrial: { label: "Industrial", icon: Factory }, agriculture: { label: "Agriculture", icon: Sprout } } satisfies Record<View, { label: string; icon: typeof Building2 }>;
const omitted = ["n/a", "unavailable", "no mapped", "no verified", "could not be checked", "needs local hazard", "needs local wildfire", "needs site-specific", "no magnitude", "preliminary only", "pending verified"];
const useful = (value: string) => Boolean(value?.trim()) && !omitted.some((phrase) => value.toLowerCase().includes(phrase));
const coordinate = (value: string | undefined, min: number, max: number) => { const parsed = Number(value); return Number.isFinite(parsed) && parsed >= min && parsed <= max ? parsed : null; };

function downloadCsv(audit: LocationAudit, climate: ClimateRisk) {
  const quote = (value: unknown) => `"${String(typeof value === "string" ? value : JSON.stringify(value)).replaceAll('"', '""')}"`;
  const rows: unknown[][] = [["period", "year", "metric", "value", "unit", "note"]];
  [["Place", audit.placeName], ["Latitude", audit.coordinates.lat], ["Longitude", audit.coordinates.lng], ["Soil nutrients", audit.soilNutrients], ["Air quality", audit.airQualityData], ["Weather", audit.weatherData], ["Disaster history", audit.disasterHistory], ["Carbon credits", audit.carbonCreditData], ["Real estate", audit.realEstateData]].forEach(([metric, value]) => rows.push(["Present audit", "", metric, value, "", "10 km audit"]));
  (climate.historical_yearly_climate ?? []).forEach((year) => [["Mean temperature", year.mean_temperature_c, "C"], ["Total precipitation", year.total_precipitation_mm, "mm"], ["Maximum daily temperature", year.maximum_daily_temperature_c, "C"], ["Maximum daily wind", year.maximum_daily_wind_kmh, "km/h"]].forEach(([metric, value, unit]) => rows.push(["Past climate", year.year, metric, value, unit, "Annual observation"])));
  (climate.climate_trigger_likelihoods ?? []).forEach((item) => rows.push(["Next-year scenario", climate.historical_period.end_year + 1, item.indicator, item.next_year_historical_likelihood_pct, "%", item.interpretation]));
  const href = URL.createObjectURL(new Blob([rows.map((row) => row.map(quote).join(",")).join("\n")], { type: "text/csv;charset=utf-8" }));
  const link = document.createElement("a"); link.href = href; link.download = `geoaudit-${audit.coordinates.lat.toFixed(4)}-${audit.coordinates.lng.toFixed(4)}.csv`; link.click(); URL.revokeObjectURL(href);
}

export default function DetailsClient({ lat: latParam, lng: lngParam, view }: { lat?: string; lng?: string; view?: string }) {
  const lat = coordinate(latParam, -90, 90); const lng = coordinate(lngParam, -180, 180);
  const [active, setActive] = useState<View>(view === "industrial" || view === "agriculture" ? view : "residential");
  const [audit, setAudit] = useState<LocationAudit | null>(null); const [error, setError] = useState<string | null>(null);
  const [climate, setClimate] = useState<ClimateRisk | null>(null); const [climateError, setClimateError] = useState<string | null>(null); const [predicting, setPredicting] = useState(false);
  useEffect(() => { if (lat === null || lng === null) return; let cancelled = false; void fetch("/api/audit", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ lat, lng }) }).then(async (response) => { const payload = await response.json(); if (!response.ok) throw new Error(payload.error ?? "Unable to load details."); if (!cancelled) setAudit(payload as LocationAudit); }).catch((reason) => !cancelled && setError(reason instanceof Error ? reason.message : "Unable to load details.")); return () => { cancelled = true; }; }, [lat, lng]);
  const predict = useCallback(async () => { if (lat === null || lng === null) return; setPredicting(true); setClimateError(null); try { const response = await fetch(`/api/climate-risk?lat=${lat}&lng=${lng}`); const payload = await response.json(); if (!response.ok) throw new Error(payload.error ?? "Climate prediction could not be created."); setClimate(payload as ClimateRisk); } catch (reason) { setClimateError(reason instanceof Error ? reason.message : "Climate prediction could not be created."); } finally { setPredicting(false); } }, [lat, lng]);
  const report = useMemo(() => { if (!audit) return null; const data = audit.aiSummary; if (active === "residential") return { decision: data.residential_view.decision, summary: data.residential_view.summary, items: [["Air quality", data.residential_view.air_quality_summary], ["Climate", data.residential_view.climate_summary], ["Flood history", data.residential_view.disaster_history.floods], ["Fire history", data.residential_view.disaster_history.fires], ["Earthquake history", data.residential_view.disaster_history.earthquakes], ["Landslide exposure", data.residential_view.disaster_history.landslides], ["Land availability", data.residential_view.local_land_availability], ["Market price / sqm", data.residential_view.estimated_market_price_range_per_sqm], ["Hospitals & facilities", data.residential_view.nearby_facilities], ["Towns & daily services", data.residential_view.nearby_towns_and_services]] }; if (active === "industrial") return { decision: data.industrial_ag_view.decision, summary: data.industrial_ag_view.summary, items: [["Soil pH", data.industrial_ag_view.soil_ph], ["Nitrogen", data.industrial_ag_view.nitrogen], ["Organic carbon", data.industrial_ag_view.organic_carbon], ["Water retention", data.industrial_ag_view.water_retention], ["Construction suitability", data.industrial_ag_view.construction_suitability], ["Industrial access & utilities", data.industrial_ag_view.industrial_access_and_utility_context], ["Carbon biomass", data.carbon_credits.vegetation_biomass_density], ["Carbon yield", `${data.carbon_credits.estimated_carbon_sequestration_potential_tco2e_per_hectare_year} tCO2e / hectare / year`], ["Carbon valuation", data.carbon_credits.estimated_offset_market_valuation_usd_per_hectare_year]] }; return { decision: data.agriculture_view.decision, summary: data.agriculture_view.summary, items: [["Soil health", data.agriculture_view.soil_health_summary], ["Climate outlook", data.agriculture_view.climate_outlook], ["Texture & structure", data.agriculture_view.texture_and_structure], ["Water management", data.agriculture_view.water_management], ["Nutrient management", data.agriculture_view.nutrient_management], ["Crop suitability", data.agriculture_view.crop_suitability], ["Latest conditions", data.agriculture_view.latest_conditions], ["Recommended next step", data.agriculture_view.recommended_next_step]] }; }, [active, audit]);
  const back = lat !== null && lng !== null ? `/?lat=${lat.toFixed(5)}&lng=${lng.toFixed(5)}` : "/";
  if (lat === null || lng === null) return <main className="min-h-screen bg-[#e6e9f5] p-6"><a href="/" className="text-sm font-bold text-blue-700">Back to map</a><p className="mt-6">Choose a location before opening its details.</p></main>;
  if (error) return <main className="min-h-screen bg-[#e6e9f5] p-6"><a href={back} className="text-sm font-bold text-blue-700">Back to map</a><p className="mt-6 text-red-700">{error}</p></main>;
  if (!audit || !report) return <main className="grid min-h-screen place-items-center bg-[#e6e9f5]"><div className="text-center"><Loader2 className="mx-auto h-7 w-7 animate-spin text-blue-600" /><p className="mt-3 text-sm font-bold">Building location report</p></div></main>;
  const caution = report.decision.toLowerCase().includes("caution");
  return <main className="min-h-screen bg-[#e6e9f5] px-4 py-5 text-slate-900 sm:px-8 sm:py-8"><div className="mx-auto max-w-6xl"><a href={back} className="inline-flex h-9 items-center gap-2 rounded-lg border border-white/70 bg-white/80 px-3 text-sm font-bold text-slate-600 shadow-sm backdrop-blur"><ArrowLeft className="h-4 w-4" />Map</a><div className="mt-6 space-y-6"><header className="flex flex-col justify-between gap-5 border-b border-slate-200 pb-6 lg:flex-row"><div><p className="text-[11px] font-bold uppercase tracking-[0.16em] text-[#1C1C1C]">10 km location intelligence</p><h1 className="mt-1 break-words text-2xl font-bold sm:text-3xl">{audit.placeName}</h1><p className="mt-3 flex items-center gap-1 text-sm font-semibold text-slate-600"><MapPin className="h-4 w-4 shrink-0 text-[#1C1C1C]" />{audit.coordinates.lat.toFixed(5)}, {audit.coordinates.lng.toFixed(5)}</p></div><div className="flex gap-2"><button onClick={predict} disabled={predicting} className="inline-flex h-9 items-center rounded-lg bg-[#1C1C1C] px-3 text-sm font-bold text-white disabled:opacity-70">{predicting ? "Creating..." : climate ? "Refresh prediction" : "Create prediction"}</button>{climate && <button onClick={() => downloadCsv(audit, climate)} title="Download past, present, and next-year data as CSV" className="grid h-9 w-9 place-items-center rounded-lg border border-slate-200 bg-white text-slate-600"><Download className="h-4 w-4" /></button>}</div></header><nav className="grid grid-cols-3 rounded-lg border border-white/70 bg-white/80 p-1 shadow-sm backdrop-blur">{(Object.keys(VIEWS) as View[]).map((key) => { const Meta = VIEWS[key]; const Icon = Meta.icon; return <button key={key} onClick={() => setActive(key)} className={`flex h-11 items-center justify-center gap-1 rounded-md text-[11px] font-bold sm:gap-2 sm:text-sm ${active === key ? "bg-[#1C1C1C] text-white" : "text-slate-500 hover:bg-white"}`}><Icon className="hidden h-4 w-4 sm:block" />{Meta.label}</button>; })}</nav><section className={`border-l-4 px-5 py-4 ${caution ? "border-amber-400 bg-amber-50 text-amber-900" : "border-[#1C1C1C] bg-white/70 text-slate-900"}`}><p className="text-[11px] font-bold uppercase tracking-[0.14em] opacity-70">{VIEWS[active].label} screening decision</p><h2 className="mt-1 text-xl font-bold">{report.decision}</h2><p className="mt-2 max-w-4xl text-sm leading-6">{report.summary}</p></section><section className="grid gap-3 md:grid-cols-2 xl:grid-cols-3">{report.items.filter(([, value]) => useful(value)).map(([label, value]) => <article key={label} className="border border-white/80 bg-white/82 p-4 shadow-sm backdrop-blur"><p className="text-[11px] font-bold uppercase tracking-[0.14em] text-slate-400">{label}</p><p className="mt-2 text-sm leading-6 text-slate-700">{value}</p></article>)}</section>{climateError && <p className="border border-red-200 bg-red-50 p-3 text-sm text-red-700">{climateError}</p>}{climate && <section className="border border-slate-300 bg-white/70 p-4 sm:p-5"><div className="flex items-start justify-between gap-3"><div><p className="text-[11px] font-bold uppercase tracking-[0.14em] text-slate-500">Past, present, and next-year climate</p><h2 className="mt-1 text-lg font-bold text-[#1C1C1C]">Historical likelihood screen</h2></div><button onClick={() => downloadCsv(audit, climate)} className="inline-flex h-8 items-center gap-2 rounded-md border border-slate-300 bg-white px-2.5 text-xs font-bold text-[#1C1C1C]"><Download className="h-3.5 w-3.5" />CSV</button></div><p className="mt-3 text-sm leading-6 text-slate-700">{climate.historical_period.start_year}-{climate.historical_period.end_year} baseline: {climate.next_year_climate_baseline.annual_mean_temperature_c} C annual mean and {climate.next_year_climate_baseline.annual_precipitation_mean_mm} mm annual precipitation.</p><div className="mt-4 grid gap-2 sm:grid-cols-2">{climate.climate_trigger_likelihoods.map((item) => <article key={item.indicator} className="border border-slate-200 bg-white p-3"><div className="flex justify-between gap-3"><p className="text-sm font-bold">{item.indicator}</p><p className="text-lg font-bold text-[#1C1C1C]">{item.next_year_historical_likelihood_pct}%</p></div><p className="mt-2 text-xs leading-5 text-slate-600">Observed in {item.historical_years_with_indicator}/{item.sample_years} years. {item.interpretation}</p></article>)}</div><p className="mt-4 text-xs leading-5 text-slate-600">{climate.important_limit}</p></section>}<p className="flex items-center gap-2 text-xs leading-5 text-slate-500"><Leaf className="h-4 w-4 text-emerald-600" />This is a 10 km screening report. Local surveys, permits, and due diligence remain required.</p></div></div></main>;
}
